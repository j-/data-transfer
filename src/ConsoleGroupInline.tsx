import React from 'react';

const ConsoleGroupInline: React.FC = ({ children }) => (
  <div className="ConsoleGroupInline d-flex flex-row">
    {children}
  </div>
);

export default ConsoleGroupInline;
