import * as React from 'react';

const MobilePaste = () => (
	<input
		type="text"
		placeholder="Use this field to paste from a mobile device"
		style={{ width: '100%' }}
	/>
);

export default MobilePaste;
