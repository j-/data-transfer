import * as React from 'react';

const MobilePaste = () => (
	<input
		type="text"
		placeholder="Use this text field to paste from a mobile device"
		style={{ width: '25em' }}
	/>
);

export default MobilePaste;
